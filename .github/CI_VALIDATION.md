# CI Validation

This file exists to exercise the repository pull-request CI workflow for PDF Hub MVP 0.1.0.

The workflow must validate both:

- FastAPI backend unit tests
- Next.js production build
