services:
  api:
    volumes:
      - ${PDFHUB_NAS_PATH:?set PDFHUB_NAS_PATH}:/data
  worker:
    volumes:
      - ${PDFHUB_NAS_PATH:?set PDFHUB_NAS_PATH}:/data
  cleanup:
    volumes:
      - ${PDFHUB_NAS_PATH:?set PDFHUB_NAS_PATH}:/data
